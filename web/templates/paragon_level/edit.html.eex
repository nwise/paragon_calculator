<h2>Edit paragon level</h2>

<%= render "form.html", changeset: @changeset,
                        action: paragon_level_path(@conn, :update, @paragon_level) %>

<%= link "Back", to: paragon_level_path(@conn, :index) %>
